<script lang="ts">
	import { createEmptyHandoffCard } from '$lib/caregrid';

	let card = $state(createEmptyHandoffCard());
</script>

<svelte:head>
	<title>CareGrid — Handoff Card</title>
</svelte:head>

<!-- Markup pending: handoff card UI is being built in a separate process. -->
